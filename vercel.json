{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@vercel/node" }],
  "routes": [

     {
      "src": "/api/(.*)", // Change this to your desired API route pattern
      "dest": "/server.js"
    },
    
    {
      "src": "/(.*)",
      "dest": "/server.js"
    }
  ]
}
