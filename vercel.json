{
    "version":2,
      "build": {
      "src": "./src",
    "output": "./dist",
    "use": "@vercel/node"
      },
    "routes":[
        {
            "src":"/(.*)",
            "dest": "/server.js"
        }
    ]
}
